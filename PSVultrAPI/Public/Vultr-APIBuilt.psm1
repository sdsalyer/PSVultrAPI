#Requires -Version 3.0

# If I follow the PSStackExchange model, there will be a psm1 file for each API call (ie. Get-VultrDNSList.psm1 or something like that)
# The actual API call will [probably] be in the private module?
function Vultr-APIBuilt {
	Write-Debug 'It Works!';
}
